# Hostinger Deployment Configuration
# DO NOT commit actual values to git!
# Set these in Hostinger cPanel → Environment Variables

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=5000
NODE_ENV=production

# ===========================================
# DATABASE CONFIGURATION
# Get these values from Hostinger cPanel → Databases
# ===========================================

# Database Host (usually 'localhost' or a specific hostname from Hostinger)
DB_HOST=localhost

# Database Username (from Hostinger MySQL panel)
DB_USER=your_database_username

# Database Password (from Hostinger MySQL panel)
DB_PASS=your_database_password

# Database Name (from Hostinger MySQL panel)
DB_NAME=your_database_name

# ===========================================
# SECURITY
# ===========================================

# JWT Secret Key for authentication (generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_this_to_a_random_secret_key

# ===========================================
# LOGGING (OPTIONAL)
# ===========================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Log directory (optional, defaults to server/logs)
# LOG_DIR=/tmp/logs

# ===========================================
# DEPLOYMENT INSTRUCTIONS
# ===========================================
#
# 1. In Hostinger cPanel, go to: Advanced → Environment Variables
# 2. Add each variable above with your actual values
# 3. For DB_HOST, DB_USER, DB_PASS, DB_NAME - get values from cPanel → Databases
# 4. After setting variables, restart your Node.js application
# 5. Visit: https://enlightenpharma.in/health to verify configuration
# 6. If database not initialized: https://enlightenpharma.in/setup-db
#
