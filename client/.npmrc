# NPM Configuration for Hostinger Deployment
# This ensures clean installs and proper production mode

# Use production mode
production=true

# Skip optional dependencies to reduce installation time
optional=false

# Disable package-lock.json updates during install
package-lock=false
